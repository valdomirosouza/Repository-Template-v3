apiVersion: v1
kind: Service
metadata:
  name: { { include "domain-service.fullname" . } }
  labels: { { - include "domain-service.labels" . | nindent 4 } }
spec:
  type: { { .Values.service.type } }
  selector: { { - include "domain-service.selectorLabels" . | nindent 4 } }
  ports:
    - name: http
      port: { { .Values.service.port } }
      targetPort: http
      protocol: TCP
